# RAG use case is project operations

The user connected RAG to a concrete PMP operating assistant: generating the project from scratch, managing modules, and following progress asynchronously from project documents. This establishes that future lessons should frame evaluation around project-operation failures, especially whether the system can correctly understand and route a "create a new module" request.
