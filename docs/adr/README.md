# ADRs

Architecture decision records capture hard-to-reverse program decisions.

Use the status and supersession format from the portfolio `build` conventions. Do not leave contradictory ADRs both looking current.
